"""Preparation checks for {{name}}."""

from __future__ import annotations

import argparse


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--decimal-purity", action="store_true")
    parser.parse_args()


if __name__ == "__main__":
    main()
