# Hand-authored baseline (recorder_sha: "00000000").
# list_branch_commits returns [{"date": iso, "message": msg}, ...] newest
# first for a branch, mirroring `gh api .../commits`. Used by StaleIssueLoop's
# branch-GC scan to age a candidate branch and search its history for a
# `Fixes #N` reference (#11418). An unseeded branch has no commit history, so
# the contract pins the empty-list shape.
adapter: github
interaction: list_branch_commits
recorded_at: "2026-08-18T00:00:00Z"
recorder_sha: "00000000"
fixture_repo: T-rav-Hydra-Ops/hydraflow-contracts-sandbox
input:
  command: list_branch_commits
  args:
    - "agent/issue-42"
  stdin: null
  env: {}
output:
  exit_code: 0
  stdout: "[]\n"
  stderr: ""
normalizers: []
baseline_only: true
