# Hand-authored baseline (recorder_sha: "00000000").
# list_all_issues returns issues in the requested state as raw gh-wire
# dicts (number/title/state/labels/createdAt/updatedAt/closedAt), mirroring
# `gh issue list --state <state> --json ...`. Used by the fitness issue
# fetcher (service_registry) and StaleIssueLoop's stale-scan / backlog-budget
# reads (#11418). A fresh fake has no issues, so the contract pins the
# empty-list shape.
adapter: github
interaction: list_all_issues
recorded_at: "2026-08-18T00:00:00Z"
recorder_sha: "00000000"
fixture_repo: T-rav-Hydra-Ops/hydraflow-contracts-sandbox
input:
  command: list_all_issues
  args: []
  stdin: null
  env: {}
output:
  exit_code: 0
  stdout: "[]\n"
  stderr: ""
normalizers: []
baseline_only: true
