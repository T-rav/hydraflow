# Hand-authored baseline (recorder_sha: "00000000").
# list_conflicting_prs returns PRs GitHub flagged mergeable=CONFLICTING
# (`gh pr list --json mergeable ...`) as ConflictingPR dataclasses
# (number/branch/labels) for merge_state_watcher. A mergeable PR is
# excluded. The fake populates ConflictingPR.branch from FakePR.branch
# (the headRefName mirror) — the former always-"" fidelity gap was fixed
# for #11595, where the auto-rebase actuator's factory-namespace scoping
# made the head branch load-bearing.
adapter: github
interaction: list_conflicting_prs
recorded_at: '2026-07-21T00:00:00Z'
recorder_sha: '00000000'
fixture_repo: T-rav-Hydra-Ops/hydraflow-contracts-sandbox
input:
  command: list_conflicting_prs
  args: []
  stdin: null
  env: {}
output:
  exit_code: 0
  stdout: '[{"number": 782, "branch": "feat/conf", "labels": []}]

    '
  stderr: ''
normalizers: []
baseline_only: true
