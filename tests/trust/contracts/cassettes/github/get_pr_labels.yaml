# Hand-authored baseline (recorder_sha: "00000000").
# get_pr_labels returns the label names carried by a GitHub pull request,
# mirroring `gh pr view <n> --json labels --jq '.labels[].name'`
# (newline-separated names). The dispatcher seeds PR #521 with a
# deterministic two-label set and asserts the newline-joined names back as
# stdout. Labels are plain strings, so no normalizer is needed.
adapter: github
interaction: get_pr_labels
recorded_at: "2026-07-26T00:00:00Z"
recorder_sha: "00000000"
fixture_repo: T-rav-Hydra-Ops/hydraflow-contracts-sandbox
input:
  command: get_pr_labels
  args:
    - "521"
  stdin: null
  env: {}
output:
  exit_code: 0
  stdout: "needs-review\npriority-high\n"
  stderr: ""
normalizers: []
baseline_only: true
