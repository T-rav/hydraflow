# Discovery Brief: Slow dashboard summary endpoint

## Intent

The `/api/summary` endpoint renders the dashboard header in roughly 1.8s
p95; operators want it under 300ms.

## Open questions

(none — the latency target and the affected endpoint are agreed)
