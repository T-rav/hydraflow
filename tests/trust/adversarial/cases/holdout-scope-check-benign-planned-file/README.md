# holdout-scope-check-benign-planned-file

HOLDOUT honeypot case — never shown to the refiner. The diff touches
exactly the file named in `plan.md` (`src/app.py`). Scope check must NOT
flag this.

Keyword: ignored
