{
  "builder": "verification_judge.VerificationJudge._build_refinement_prompt",
  "args": {
    "instructions": "1. Start the orchestrator against the sandbox fleet with\n   `make sandbox-up`, then open the run dashboard at http://localhost:8080/dashboard\n   and confirm the red escalation banner is not visible on a clean queue.\n2. Label sandbox issue #4821 `hydraflow-hitl`. Within one 5s poll the banner\n   should appear reading \"1 issue(s) awaiting human input\" with a `#4821` list\n   item underneath it.\n3. Label a second sandbox issue `hydraflow-hitl`. The banner count should go to\n   2 and both issue numbers should be listed in ascending order.\n4. Remove the `hydraflow-hitl` label from #4821 only. Do not reload the page.\n   Within 5s the count should drop to 1 and the `#4821` row should disappear\n   while the second issue stays listed.\n5. Remove the label from the second issue, again without reloading. The banner\n   should disappear entirely within 5s and the list container should be empty.\n6. With the queue clear, call `curl -i http://localhost:8080/api/escalations`\n   and confirm the response is `HTTP/1.1 200 OK` with a body of exactly `[]`.\n7. Check the orchestrator log for one `Escalation for issue #4821 resolved`\n   line per resolved issue and no repeats on subsequent polls.\n8. Reload the dashboard once at the end and confirm the banner is still hidden,\n   proving the cleared state came from the live payload rather than stale\n   client cache.",
    "feedback": "The instructions are close to ready but three steps\ncannot be followed without guessing:\n\n1. Step 1 says \"confirm the red escalation banner is not visible on a clean\n   queue\" but never says how to get a clean queue. If a previous run left an\n   issue labelled `hydraflow-hitl`, the tester sees a banner at step 1 and has\n   no stated way to tell that from a failure. Name the precondition explicitly\n   (no open issue carries the `hydraflow-hitl` label) and give the command that\n   checks it.\n2. Steps 3 through 5 say \"a second sandbox issue\" without naming it. Two\n   testers will pick two different issues, and step 4's \"the second issue stays\n   listed\" assertion is then unverifiable from the text alone. Use a concrete\n   issue number the way step 2 does.\n3. Step 7 says to check \"the orchestrator log\" without saying where that log\n   is or what command surfaces it. On the sandbox fleet the log is not on\n   stdout, so this step is a dead end as written.\n\nAlso missing: no step covers the negative case where the label is removed while\nthe dashboard tab is backgrounded, which is how the original bug was reported.\nExpected outcomes for steps 6 and 8 are good and should be left as they are.",
    "issue_number": 4821
  },
  "faked_deps": {}
}
