{
  "builder": "shape_coherence.build_shape_coherence_prompt",
  "args": {
    "issue_number": 4711,
    "issue_title": "Retry transient S3 throttling in the archive uploader",
    "discover_brief": "The archive uploader has no retry path. S3 throttling (SlowDown) is\nexpected under burst load and is documented as retryable. Two call sites use\n`upload_with_retry`; both are batch paths where a bounded retry is safe. No\nidempotency concern: put_object with the same key is idempotent.\n",
    "proposal": "Bounded retry with exponential backoff on a frozenset of retryable S3 codes; typed UploadFailed on exhaustion.",
    "guidance": "Keep the retry bound configurable; do not swallow non-retryable errors."
  },
  "faked_deps": {}
}
