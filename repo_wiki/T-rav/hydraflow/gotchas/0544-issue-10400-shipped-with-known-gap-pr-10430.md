---
id: 0544
topic: gotchas
source_issue: 10400
source_phase: legacy-migrated
created_at: 2026-07-24T08:45:37.274495+00:00
status: stale
corroborations: 1
stale_reason: source issue #10400 closed
---

# Shipped with known gap — PR #10430

# Shipped with known gap — PR #10430

PR #10430 merged with 24 unresolved adversarial concern(s) that survived all gates without an explicit ConcernResolution. Future planners / reviewers should treat these as known gaps until either addressed in a follow-up PR or explicitly accepted.

## Unresolved concerns

- **HIGH** `PLAN-ASSUMP-001` (raised in `plan` / `assumption_surfacer`): Mechanism assumption: the regex parsing behavior for colon-delimited `path:symbol` format is untested; validate that _SOURCE_FILE_CITATION_RE correctly extracts both file and symbol names from the new format in a scratch repo before committing to this mechanism in the plan
  - _must_address_by_: `planner`
- **HIGH** `PLAN-ASSUMP-002` (raised in `plan` / `assumption_surfacer`): Verify that ADR-0011/0080/0081 actively use `src/epic.py:method` format and that their symbol-level citations pass the adr_index drift check (prove the pattern works, not just that it exists)
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-ASSUMP-003` (raised in `plan` / `assumption_surfacer`): Search ADR-0012 full text for all separate-backtick citation patterns (not just line 185); incomplete conversion will leave some citations bare and subject to false drift
  - _must_address_by_: `planner`
- **HIGH** `PLAN-ASSUMP-001` (raised in `plan` / `assumption_surfacer`): Verify that the methods cited in ADR-0012 line 185 (EpicManager.on_child_approved, etc.) still exist in current src/epic.py with exact names; if any were refactored/renamed, the citations become dangling after the format conversion. Mechanism assumption: method names in live code don't drift independently from documentation.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-ASSUMP-002` (raised in `plan` / `assumption_surfacer`): The carryover concern PLAN-ASSUMP-001 flags that regex parsing of `path:symbol` format is untested. Before marking the planner's validation complete, confirm the test case covers edge cases: symbols with dots (e.g., `EpicManager.Nested.method`), multiple symbols on one line, and symbol-less bare paths in the same ADR.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-ASSUMP-003` (raised in `plan` / `assumption_surfacer`): Search the entire repo documentation tree (wiki, docs/superpowers/, docs/methodology/, etc.) for the same separate-backtick citation anti-pattern, not just ADRs. A systematic fix may be more efficient than patching ADR-0012 in isolation.
  - _must_address_by_: `planner`
- **LOW** `PLAN-ASSUMP-004` (raised in `plan` / `assumption_surfacer`): Verify that no tooling, link checkers, or external systems depend on the old bare-citation format `src/epic.py (method1, method2)` before rewriting; unlikely but worth a quick grep across tools/ or CI config.
  - _must_address_by_: `planner`
- **CRITICAL** `PLAN-TESTER-001` (raised in `plan` / `plan_council_tester`): Regex parsing untested in isolation. The plan assumes `_SOURCE_FILE_CITATION_RE` correctly extracts both file and symbol parts from the new `path:Symbol` format (e.g., `src/epic.py:EpicManager.on_child_approved`), but no explicit unit test for the regex is described. PLAN-ASSUMP-001 flags this risk, yet the plan proceeds. Acceptance criterion: test that the regex correctly parses edge cases (qualified symbols with dots, multiple symbols on one line, symbol-less paths in the same ADR).
  - _must_address_by_: `planner`
- **CRITICAL** `PLAN-TESTER-002` (raised in `plan` / `plan_council_tester`): Parametrized regression test assertions not detailed. The plan says 'extend the parametrized harness' and 'verify ADR-0012 no longer drifts on file-only epic.py churn,' but the specific test code/assertions are not shown. Acceptance criterion: describe the exact assertion (e.g., `assert compute_drift(adr_dict, file_only_diff) does not include ADR-0012`) and verify it actually fails without the symbol-qualified citation.
  - _must_address_by_: `planner`
- **HIGH** `PLAN-TESTER-003` (raised in `plan` / `plan_council_tester`): Method existence not validated by automated test. The plan claims all four methods (`on_child_approved`, `_handle_bundled_ready`, `_handle_ordered_ready`, `_get_merge_order`) 'are verified EpicManager methods,' but no test will fail if these methods are renamed or deleted from `src/epic.py`. Acceptance criterion: `test_live_adr_source_citations_resolve` should validate that each symbol in the citation actually exists in the codebase, not just that the citation parses.
  - _must_address_by_: `planner`
- **HIGH** `PLAN-TESTER-004` (raised in `plan` / `plan_council_tester`): Incomplete citation search within ADR-0012. PLAN-ASSUMP-003 (pending concerns) warns that ADR-0012 may have additional separate-backtick citation patterns beyond line 185. The plan narrows scope to 'ONLY line 185' but does not describe a test that validates this is exhaustive. Acceptance criterion: add a test assertion that ADR-0012 contains zero bare (non-symbol-qualified) `src/epic.py` citations after the edit.
  - _must_address_by_: `planner`
- **HIGH** `PLAN-TESTER-005` (raised in `plan` / `plan_council_tester`): Symbol-inertness integration not explicitly tested. The plan assumes that non-empty `source_symbols` makes the citation inert to file-only diffs via `adr_drift._citation_drifts`, but no test explicitly verifies this integration—only that the parametrized case 'passes.' Acceptance criterion: add a test that compares drift results with an empty symbol set vs. populated symbol set for the same file-only diff.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-TESTER-006` (raised in `plan` / `plan_council_tester`): Adjacent lines 184/186 stability not guarded. The plan states lines 184/186 (`post_merge_handler.py`, `models.py`) should remain unchanged, but no automated test prevents accidental modification. Acceptance criterion: add an assertion that the unmodified lines in the regression test are identical before and after the edit (e.g., snapshot test or explicit line checks).
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-TESTER-007` (raised in `plan` / `plan_council_tester`): Backward-compat artifact stability not tested. The plan claims `make arch-regen` produces zero diff because `adr_xref.md` lists modules, not symbols, but this is verified manually ('confirm `make arch-regen` produces zero diff'), not as an automated test. Acceptance criterion: add a pre/post `make arch-regen` diff check within the test suite or as a CI assertion.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-TESTER-008` (raised in `plan` / `plan_council_tester`): Symbol qualification convention not validated. The plan assumes `EpicManager.<method>` matches ADR-0005/0009 convention and the regex's dotted-symbol support, but no test verifies the format is consistent with those ADRs or that the regex actually supports dots. Acceptance criterion: grep ADR-0005 and ADR-0009 for symbol examples and add a test case validating the same pattern is used in ADR-0012.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-TESTER-009` (raised in `plan` / `plan_council_tester`): Docstring extension clarity. The plan says 'extend the module docstring to note ADR-0012 joined the right-sized set' in `test_issue_9419_9421_adr_drift.py`, but no test validates that this docstring update is accurate or complete. Acceptance criterion: the docstring should list all ADRs in `_RIGHT_SIZED`, and a test could enumerate the set and cross-check against the docstring.
  - _must_address_by_: `planner`
- **LOW** `PLAN-TESTER-010` (raised in `plan` / `plan_council_tester`): Wider documentation tree not scanned. PLAN-ASSUMP-003 (pending concerns) suggests searching the entire repo documentation tree (wiki, superpowers, methodology) for the same separate-backtick anti-pattern. The plan siloes the fix to ADR-0012 only. Acceptance criterion: run a grep to confirm no other ADRs or docs have the same pattern, or widen the fix scope.
  - _must_address_by_: `planner`
- **CRITICAL** `PLAN-RISK_SKEPTIC-001` (raised in `plan` / `plan_council_risk_skeptic`): Untested regex mechanism assumption. The plan hinges on `_SOURCE_FILE_CITATION_RE` correctly parsing `path:Symbol` format with colon delimiters, but this behavior is not empirically validated in the plan or pending concerns. PLAN-ASSUMP-001 (HIGH) flags this explicitly. Before committing to this mechanism, a scratch-repo test must demonstrate: (1) the regex correctly extracts both file and symbol components from `src/epic.py:EpicManager.<method>`, (2) it still handles bare paths without colons (backward compat), (3) edge cases like symbols with dots (`Nested.method`) parse correctly. Untested mechanism assumptions are a top-3 source of implementation failures per Risk-Skeptic charter.
  - _must_address_by_: `planner`
- **HIGH** `PLAN-RISK_SKEPTIC-002` (raised in `plan` / `plan_council_risk_skeptic`): Method existence not verified before citation format conversion. The plan asserts 'all four cited methods are verified EpicManager methods' but provides no evidence: no grep results, no current code snippet, no verification date. If any of `on_child_approved`, `_handle_bundled_ready`, `_handle_ordered_ready`, `_get_merge_order` were refactored/renamed, the new citations become dangling. The verification must happen before the doc edit, not after. Add a pre-implementation spike: `grep -E '(on_child_approved|_handle_bundled_ready|_handle_ordered_ready|_get_merge_order)' src/epic.py` with results included in the plan.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-RISK_SKEPTIC-003` (raised in `plan` / `plan_council_risk_skeptic`): Incomplete citation search. PLAN-ASSUMP-003 (MEDIUM) warns that the plan only addresses line 185 but ADR-0012 may contain other separate-backtick citation patterns elsewhere. A systematic grep of ADR-0012's full text for the pattern `\`[^`]+\)` (backtick-path-bare, not backtick-path-symbol) is mandatory before starting implementation, to ensure the fix is exhaustive within this ADR and prevent re-drift.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-RISK_SKEPTIC-004` (raised in `plan` / `plan_council_risk_skeptic`): Scope boundary ambiguity—one ADR or pattern audit? PLAN-ASSUMP-003 suggests searching 'the entire repo documentation tree' for the same anti-pattern. The plan narrows to 'only `epic.py`' and 'only ADR-0012', but doesn't explain why this isolated fix is better than a systematic audit of all ADRs (and wiki/superpowers/methodology docs) for the same citation drift vulnerability. If 5+ other ADRs have the same bare-citation pattern, shipping a one-off fix is technical debt multiplied. Either justify isolation or scope the systematic fix upfront.
  - _must_address_by_: `planner`
- **MEDIUM** `PLAN-RISK_SKEPTIC-005` (raised in `plan` / `plan_council_risk_skeptic`): Test case specificity underspecified. The plan reuses existing parametrized harness in `test_issue_9419_9421_adr_drift.py`, reducing duplication—good. But the Acceptance Criteria do not explicitly list which test parameters/cases validate this change. Insert before implementation: 'Add test case: ADR-0012 with `source_symbols['src/epic.py'] = {"EpicManager"}`; verify file-only src/epic.py diff → no drift finding.' Without explicit case naming, edge cases (empty symbol set, symbol-less fallback, multi-symbol lines) may slip through the parametrized harness.
  - _must_address_by_: `planner`
- **LOW** `PLAN-RISK_SKEPTIC-006` (raised in `plan` / `plan_council_risk_skeptic`): Backward compatibility—downstream citation format readers. The plan assumes no external tooling, link checkers, or CI scripts depend on the old bare-citation format `src/epic.py (method1, method2)`. PLAN-ASSUMP-004 (LOW) flags this. A quick grep of `tools/`, CI config (`.github/workflows/`, `scripts/`), and Markdown link-checker configs for patterns like `\[.*epic\.py.*\]` or hardcoded citation format assumptions is prudent before rewriting.
  - _must_address_by_: `planner`
- **HIGH** `SPEC-JUDGE-001` (raised in `plan` / `spec_judge`): SpecJudge could not parse agent output: Invalid \escape: line 18 column 444 (char 2667)
  - _must_address_by_: `implement`

```json:entry
{"id": "shipped-with-known-gap-pr-10430", "title": "Shipped with known gap \u2014 PR #10430", "topic": "gotchas", "source_type": "shipped-with-known-gap", "source_issue": null, "source_repo": null, "pr_number": 10430, "created_at": "2026-07-24T08:45:37.274383+00:00", "updated_at": "2026-07-24T08:45:37.274383+00:00", "valid_to": null, "superseded_by": null, "superseded_reason": null, "confidence": "high", "stale": false, "corroborations": 1, "concerns": [{"id": "PLAN-ASSUMP-001", "severity": "HIGH", "concern": "Mechanism assumption: the regex parsing behavior for colon-delimited `path:symbol` format is untested; validate that _SOURCE_FILE_CITATION_RE correctly extracts both file and symbol names from the new format in a scratch repo before committing to this mechanism in the plan", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-002", "severity": "HIGH", "concern": "Verify that ADR-0011/0080/0081 actively use `src/epic.py:method` format and that their symbol-level citations pass the adr_index drift check (prove the pattern works, not just that it exists)", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-003", "severity": "MEDIUM", "concern": "Search ADR-0012 full text for all separate-backtick citation patterns (not just line 185); incomplete conversion will leave some citations bare and subject to false drift", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-001", "severity": "HIGH", "concern": "Verify that the methods cited in ADR-0012 line 185 (EpicManager.on_child_approved, etc.) still exist in current src/epic.py with exact names; if any were refactored/renamed, the citations become dangling after the format conversion. Mechanism assumption: method names in live code don't drift independently from documentation.", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-002", "severity": "MEDIUM", "concern": "The carryover concern PLAN-ASSUMP-001 flags that regex parsing of `path:symbol` format is untested. Before marking the planner's validation complete, confirm the test case covers edge cases: symbols with dots (e.g., `EpicManager.Nested.method`), multiple symbols on one line, and symbol-less bare paths in the same ADR.", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-003", "severity": "MEDIUM", "concern": "Search the entire repo documentation tree (wiki, docs/superpowers/, docs/methodology/, etc.) for the same separate-backtick citation anti-pattern, not just ADRs. A systematic fix may be more efficient than patching ADR-0012 in isolation.", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-ASSUMP-004", "severity": "LOW", "concern": "Verify that no tooling, link checkers, or external systems depend on the old bare-citation format `src/epic.py (method1, method2)` before rewriting; unlikely but worth a quick grep across tools/ or CI config.", "raised_in_phase": "plan", "raised_in_stage": "assumption_surfacer", "must_address_by": "planner"}, {"id": "PLAN-TESTER-001", "severity": "CRITICAL", "concern": "Regex parsing untested in isolation. The plan assumes `_SOURCE_FILE_CITATION_RE` correctly extracts both file and symbol parts from the new `path:Symbol` format (e.g., `src/epic.py:EpicManager.on_child_approved`), but no explicit unit test for the regex is described. PLAN-ASSUMP-001 flags this risk, yet the plan proceeds. Acceptance criterion: test that the regex correctly parses edge cases (qualified symbols with dots, multiple symbols on one line, symbol-less paths in the same ADR).", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-002", "severity": "CRITICAL", "concern": "Parametrized regression test assertions not detailed. The plan says 'extend the parametrized harness' and 'verify ADR-0012 no longer drifts on file-only epic.py churn,' but the specific test code/assertions are not shown. Acceptance criterion: describe the exact assertion (e.g., `assert compute_drift(adr_dict, file_only_diff) does not include ADR-0012`) and verify it actually fails without the symbol-qualified citation.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-003", "severity": "HIGH", "concern": "Method existence not validated by automated test. The plan claims all four methods (`on_child_approved`, `_handle_bundled_ready`, `_handle_ordered_ready`, `_get_merge_order`) 'are verified EpicManager methods,' but no test will fail if these methods are renamed or deleted from `src/epic.py`. Acceptance criterion: `test_live_adr_source_citations_resolve` should validate that each symbol in the citation actually exists in the codebase, not just that the citation parses.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-004", "severity": "HIGH", "concern": "Incomplete citation search within ADR-0012. PLAN-ASSUMP-003 (pending concerns) warns that ADR-0012 may have additional separate-backtick citation patterns beyond line 185. The plan narrows scope to 'ONLY line 185' but does not describe a test that validates this is exhaustive. Acceptance criterion: add a test assertion that ADR-0012 contains zero bare (non-symbol-qualified) `src/epic.py` citations after the edit.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-005", "severity": "HIGH", "concern": "Symbol-inertness integration not explicitly tested. The plan assumes that non-empty `source_symbols` makes the citation inert to file-only diffs via `adr_drift._citation_drifts`, but no test explicitly verifies this integration\u2014only that the parametrized case 'passes.' Acceptance criterion: add a test that compares drift results with an empty symbol set vs. populated symbol set for the same file-only diff.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-006", "severity": "MEDIUM", "concern": "Adjacent lines 184/186 stability not guarded. The plan states lines 184/186 (`post_merge_handler.py`, `models.py`) should remain unchanged, but no automated test prevents accidental modification. Acceptance criterion: add an assertion that the unmodified lines in the regression test are identical before and after the edit (e.g., snapshot test or explicit line checks).", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-007", "severity": "MEDIUM", "concern": "Backward-compat artifact stability not tested. The plan claims `make arch-regen` produces zero diff because `adr_xref.md` lists modules, not symbols, but this is verified manually ('confirm `make arch-regen` produces zero diff'), not as an automated test. Acceptance criterion: add a pre/post `make arch-regen` diff check within the test suite or as a CI assertion.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-008", "severity": "MEDIUM", "concern": "Symbol qualification convention not validated. The plan assumes `EpicManager.<method>` matches ADR-0005/0009 convention and the regex's dotted-symbol support, but no test verifies the format is consistent with those ADRs or that the regex actually supports dots. Acceptance criterion: grep ADR-0005 and ADR-0009 for symbol examples and add a test case validating the same pattern is used in ADR-0012.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-009", "severity": "MEDIUM", "concern": "Docstring extension clarity. The plan says 'extend the module docstring to note ADR-0012 joined the right-sized set' in `test_issue_9419_9421_adr_drift.py`, but no test validates that this docstring update is accurate or complete. Acceptance criterion: the docstring should list all ADRs in `_RIGHT_SIZED`, and a test could enumerate the set and cross-check against the docstring.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-TESTER-010", "severity": "LOW", "concern": "Wider documentation tree not scanned. PLAN-ASSUMP-003 (pending concerns) suggests searching the entire repo documentation tree (wiki, superpowers, methodology) for the same separate-backtick anti-pattern. The plan siloes the fix to ADR-0012 only. Acceptance criterion: run a grep to confirm no other ADRs or docs have the same pattern, or widen the fix scope.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_tester", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-001", "severity": "CRITICAL", "concern": "Untested regex mechanism assumption. The plan hinges on `_SOURCE_FILE_CITATION_RE` correctly parsing `path:Symbol` format with colon delimiters, but this behavior is not empirically validated in the plan or pending concerns. PLAN-ASSUMP-001 (HIGH) flags this explicitly. Before committing to this mechanism, a scratch-repo test must demonstrate: (1) the regex correctly extracts both file and symbol components from `src/epic.py:EpicManager.<method>`, (2) it still handles bare paths without colons (backward compat), (3) edge cases like symbols with dots (`Nested.method`) parse correctly. Untested mechanism assumptions are a top-3 source of implementation failures per Risk-Skeptic charter.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-002", "severity": "HIGH", "concern": "Method existence not verified before citation format conversion. The plan asserts 'all four cited methods are verified EpicManager methods' but provides no evidence: no grep results, no current code snippet, no verification date. If any of `on_child_approved`, `_handle_bundled_ready`, `_handle_ordered_ready`, `_get_merge_order` were refactored/renamed, the new citations become dangling. The verification must happen before the doc edit, not after. Add a pre-implementation spike: `grep -E '(on_child_approved|_handle_bundled_ready|_handle_ordered_ready|_get_merge_order)' src/epic.py` with results included in the plan.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-003", "severity": "MEDIUM", "concern": "Incomplete citation search. PLAN-ASSUMP-003 (MEDIUM) warns that the plan only addresses line 185 but ADR-0012 may contain other separate-backtick citation patterns elsewhere. A systematic grep of ADR-0012's full text for the pattern `\\`[^`]+\\)` (backtick-path-bare, not backtick-path-symbol) is mandatory before starting implementation, to ensure the fix is exhaustive within this ADR and prevent re-drift.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-004", "severity": "MEDIUM", "concern": "Scope boundary ambiguity\u2014one ADR or pattern audit? PLAN-ASSUMP-003 suggests searching 'the entire repo documentation tree' for the same anti-pattern. The plan narrows to 'only `epic.py`' and 'only ADR-0012', but doesn't explain why this isolated fix is better than a systematic audit of all ADRs (and wiki/superpowers/methodology docs) for the same citation drift vulnerability. If 5+ other ADRs have the same bare-citation pattern, shipping a one-off fix is technical debt multiplied. Either justify isolation or scope the systematic fix upfront.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-005", "severity": "MEDIUM", "concern": "Test case specificity underspecified. The plan reuses existing parametrized harness in `test_issue_9419_9421_adr_drift.py`, reducing duplication\u2014good. But the Acceptance Criteria do not explicitly list which test parameters/cases validate this change. Insert before implementation: 'Add test case: ADR-0012 with `source_symbols['src/epic.py'] = {\"EpicManager\"}`; verify file-only src/epic.py diff \u2192 no drift finding.' Without explicit case naming, edge cases (empty symbol set, symbol-less fallback, multi-symbol lines) may slip through the parametrized harness.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "PLAN-RISK_SKEPTIC-006", "severity": "LOW", "concern": "Backward compatibility\u2014downstream citation format readers. The plan assumes no external tooling, link checkers, or CI scripts depend on the old bare-citation format `src/epic.py (method1, method2)`. PLAN-ASSUMP-004 (LOW) flags this. A quick grep of `tools/`, CI config (`.github/workflows/`, `scripts/`), and Markdown link-checker configs for patterns like `\\[.*epic\\.py.*\\]` or hardcoded citation format assumptions is prudent before rewriting.", "raised_in_phase": "plan", "raised_in_stage": "plan_council_risk_skeptic", "must_address_by": "planner"}, {"id": "SPEC-JUDGE-001", "severity": "HIGH", "concern": "SpecJudge could not parse agent output: Invalid \\escape: line 18 column 444 (char 2667)", "raised_in_phase": "plan", "raised_in_stage": "spec_judge", "must_address_by": "implement"}]}
```

